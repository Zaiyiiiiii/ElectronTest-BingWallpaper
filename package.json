{
  "name": "BingWallPaper-Electron",
  "version": "0.0.1",
  "description": "A Demo",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "electron-packager ./ --platform=win32 --overwrite",
    "pack": "build --dir",
    "dist": "build"
  },
  "repository": {
    "type": "git",
    "url": "no"
  },
  "author": "wangzaiyi",
  "license": "ISC",
  "build": {
    "appId": "your.id",
    "mac": {
      "category": "your.app.category.type"
    },
    "win": {
      "iconUrl": "(windows-only) https link to icon"
    }
  },
  "devDependencies": {
    "electron-builder": "^8.6.0",
    "electron-packager": "^8.3.0",
    "electron-prebuilt": "^1.4.6",
    "electron-rebuild": "^1.4.0"
  },
  "dependencies": {
    "ffi": "^2.2.0"
  }
}